# 用户注册登录指南

## 注册账号

### 表单验证规则

注册时需要填写以下信息，每个字段都有特定的格式要求：

#### 1. 用户名

- **长度要求**: 2-20个字符
- **示例**:
  - ✅ 正确: `小明`, `张三`, `admin123`, `user_name`
  - ❌ 错误: `a` (太短), `这是一个超过二十个字符的用户名` (太长)

#### 2. 邮箱地址

- **格式要求**: 必须包含 `@` 和 `.`，符合标准邮箱格式
- **示例**:
  - ✅ 正确: `user@example.com`, `zhangsan@qq.com`, `test123@gmail.com`
  - ❌ 错误: `user`, `user@`, `@example.com`, `user.example.com`

#### 3. 密码

- **长度要求**: 6-50个字符
- **示例**:
  - ✅ 正确: `123456`, `password`, `mySecret2024`
  - ❌ 错误: `12345` (太短，少于6位)

### 完整的注册示例

```
用户名: 张三
邮箱: zhangsan@example.com
密码: password123
```

## 登录账号

登录只需要填写：

- **邮箱地址**: 注册时使用的邮箱
- **密码**: 注册时设置的密码

## 常见错误提示

### "请输入有效的邮箱地址（例如：user@example.com）"

**原因**: 邮箱格式不正确
**解决方法**:

1. 确保邮箱包含 `@` 符号
2. 确保 `@` 后面有域名（如 `example.com`）
3. 完整格式: `用户名@域名.后缀`

**正确示例**:

- `test@qq.com`
- `user@163.com`
- `admin@company.com.cn`

### "用户名长度应在2-20个字符之间"

**原因**: 用户名太短或太长
**解决方法**: 确保用户名至少2个字符，最多20个字符

### "密码长度至少为6个字符"

**原因**: 密码太短
**解决方法**: 设置至少6位数的密码

### "邮箱已被注册"

**原因**: 该邮箱已经被其他用户使用
**解决方法**:

1. 使用其他邮箱注册
2. 或者如果是您自己的邮箱，请直接登录

### "网络连接失败，请检查后端服务器是否启动（端口4000）"

**原因**: 无法连接到后端服务器
**解决方法**:

1. 确认后端服务器已启动
2. 检查浏览器控制台（F12）查看详细错误信息
3. 查看 `DEBUGGING_AUTH.md` 文件进行深度调试

## 注册流程

1. **打开网站首页**
2. **点击"注册"按钮** （右上角）
3. **填写注册表单**:
   - 用户名: 2-20个字符
   - 邮箱: 标准邮箱格式
   - 密码: 至少6个字符
4. **点击"注册"按钮**
5. **等待处理**:
   - 按钮会显示"处理中..."
   - 成功后自动跳转到编辑器页面
6. **开始使用**

## 登录流程

1. **打开网站首页**
2. **点击"登录"按钮** （右上角）
3. **填写登录表单**:
   - 邮箱: 注册时使用的邮箱
   - 密码: 注册时设置的密码
4. **点击"登录"按钮**
5. **等待处理**:
   - 按钮会显示"处理中..."
   - 成功后自动跳转到编辑器页面
6. **开始使用**

## 快速测试账号

如果您想快速测试系统，可以使用以下格式创建测试账号：

```
用户名: test123
邮箱: test123@example.com
密码: 123456
```

注意：每个邮箱只能注册一次，如果提示"邮箱已被注册"，请更换其他邮箱。

## 调试信息

如果遇到问题，请：

1. 按 `F12` 打开浏览器开发者工具
2. 切换到 "Console" 标签
3. 查看以 `[API Config]` 和 `[Auth]` 开头的日志
4. 参考 `DEBUGGING_AUTH.md` 文件进行详细排查

## 功能特点

- ✅ **强制登录**: 未登录用户无法访问编辑器，点击"开始创作"会自动弹出登录框
- ✅ **云端存储**: 所有文档自动保存到云端，随时随地访问
- ✅ **实时验证**: 表单填写时会实时检查格式，提前发现问题
- ✅ **友好提示**: 每个错误都有清晰的中文说明和解决方法
- ✅ **安全加密**: 密码使用bcrypt加密存储，保障账号安全
