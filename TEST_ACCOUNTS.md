# 🔐 测试账号信息

## 可直接使用的测试账号

以下账号已预先创建在系统中，**可以直接登录，无需注册**：

---

### 账号 1：通用测试账号 ⭐ 推荐

```
邮箱：test@example.com
密码：123456
用户名：测试用户
```

**推荐用于一般测试和演示**

---

### 账号 2：Demo演示账号

```
邮箱：demo@wemd.com
密码：demo123
用户名：Demo用户
```

**适用于产品演示和功能展示**

---

### 账号 3：管理员测试账号

```
邮箱：admin@test.com
密码：admin888
用户名：管理员
```

**用于权限和管理功能测试**

---

## 使用说明

### 在 iPad 上登录

1. 打开应用登录页面
2. 点击 **"登录"** 标签（如果显示的是注册）
3. 输入以下信息（推荐使用账号1）：
   - **邮箱**：`test@example.com`
   - **密码**：`123456`
4. 点击 **"登录"** 按钮
5. 登录成功！🎉

### 在电脑端登录

同样的步骤，使用上述任意一组账号密码即可。

---

## 测试账号的数据存储

- ✅ 测试账号已预先创建并加载到服务器内存中
- ✅ 可以立即登录，无需注册
- ✅ 每个账号都有独立的云端存储空间
- ⚠️ 注意：服务器重启后，测试账号会保留，但用户数据可能会重置（因为使用内存存储）

---

## 验证测试账号是否可用

### 方法1：直接登录（推荐）

在登录页面使用上述任意账号尝试登录。

### 方法2：通过API检查

在浏览器中访问：

```
http://localhost:4000/api/auth/users
```

或在 Clacky 平台上访问：

```
https://你的域名-4000.clackypaas.com/api/auth/users
```

应该会看到 3 个测试用户的列表。

---

## 常见问题

### Q: 为什么需要测试账号？

A: 这样可以避免每次测试都需要注册新账号，也避免了表单验证可能遇到的问题。

### Q: 测试账号的密码安全吗？

A: 密码已经使用 bcrypt 加密存储，与正常注册的用户使用相同的安全机制。

### Q: 可以修改测试账号的密码吗？

A: 当前版本不支持修改密码功能。如果需要，可以使用其他测试账号。

### Q: 测试账号会被其他人使用吗？

A: 是的，这些是公共测试账号。如果需要私密数据，请注册新账号（注册功能仍然可用）。

### Q: 服务器重启后数据会丢失吗？

A: 测试账号本身会保留，但存储的文档数据可能会丢失（因为使用内存存储）。

---

## 技术实现细节

测试账号定义在以下文件中：

- `apps/server/src/auth/test-users.ts` - 测试用户配置
- `apps/server/src/auth/auth.service.ts` - 服务启动时自动加载

服务器启动日志会显示：

```
✅ 测试用户已加载: [
  { email: 'test@example.com', username: '测试用户' },
  { email: 'demo@wemd.com', username: 'Demo用户' },
  { email: 'admin@test.com', username: '管理员' }
]
```

---

## 快速测试清单

- [ ] 使用 `test@example.com` / `123456` 登录
- [ ] 创建新文档
- [ ] 编辑并保存文档
- [ ] 退出登录
- [ ] 使用另一个测试账号登录
- [ ] 验证数据隔离（不同账号看不到对方的数据）

---

**现在您可以直接使用 `test@example.com` / `123456` 登录，无需担心表单验证问题！** 🚀
